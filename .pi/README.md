# Archived Pi runtime pointer

This ~/.pi directory is no longer Monika runtime source of truth.

Current runtime source of truth:

- Code, bundled extensions, persona defaults: ~/repos/monika
- Durable runtime state: ~/repos/monika/runtime
- Deployment file: ~/repos/monika/compose.yaml copied from compose.yaml.example

The former host-mode ~/.pi tree was moved to:

/home/monika/ARCHIVE_DO_NOT_USE_old_host_mode_pi_2026-06-17

Do not edit that archive as live configuration. It is retained only for rollback/reference.
